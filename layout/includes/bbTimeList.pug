if site.data.essay
  each i in site.data.essay
    if i.home_essay
      - let onclick_value = theme.pjax.enable ? `pjax.loadUrl("/feed/");` : '';
      - let href_value = theme.pjax.enable ? 'javascript:void(0);' : `/feed/`;
      #bbTimeList.bbTimeList.container
          i.anzhiyufont.anzhiyu-icon-angle-right.fontbold(onclick=onclick_value, title="Feed", href=href_value, aria-hidden="true")
          #bbtalk.swiper-container.swiper-no-swiping.essay_bar_swiper_container(tabindex="-1")
            #bber-talk.swiper-wrapper(onclick=onclick_value)
              each i in site.data.essay
                each item, index in i.essay_list
                  if index < 10
                    - var contentText = item.image ? item.content + ' [Image]' : (item.video ? item.content + ' [Video]' : item.content)
                    a.li-style.swiper-slide(href=href_value)= contentText
          a.bber-gotobb.anzhiyufont.anzhiyu-icon-circle-arrow-right( onclick=onclick_value, href=href_value, title="Read More")

      script(src=url_for(theme.home_top.swiper.swiper_js))
